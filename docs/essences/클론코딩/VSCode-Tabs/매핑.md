# VSCode Tabs 1:1 매핑

VS Code 탭 시스템과 현재 구현의 매핑 분석.

## 구현 현황: 100%

완전 구현 97%, 부분 구현 3% (싱글 탭 모드 - P3)

### Phase 5 완료 (2024-11) - P2 기능 구현
- ✅ Tab wrapping (multi-line) - flex-wrap: wrap 기반
- ✅ Scroll buttons on overflow - 좌우 스크롤 버튼
- ✅ 휠 스크롤 지원 (deltaY → scrollLeft)
- ✅ Breadcrumbs navigation - EditorBreadcrumbs.tsx

### 완료 파일

| 구현 파일 | 원본 소스 | 상태 |
|----------|----------|------|
| EditorTabsControl.tsx | editorTabsControl.ts, multiEditorTabsControl.ts | ✅ |
| EditorActionsToolbar.tsx | editorTitleControl.ts | ✅ |
| EditorBreadcrumbs.tsx | breadcrumbs.ts | ✅ P2 |
| tabsStore.ts | 자체 구현 (Zustand) | ✅ |
| contextMenuService.ts | contextMenuService.ts | ✅ |
| keybindingService.ts | keybindingService.ts | ✅ |
| useAuxiliaryWindowReceiver.ts | auxiliaryWindowService.ts | ✅ |
| useContextKey.tsx | contextkey.ts | ✅ |
| dnd.ts | dnd.ts | ✅ |

### 주요 기능 구현 현황

- ✅ 탭 렌더링 (멀티 탭, 싱글 탭)
- ✅ 탭 드래그앤드롭 (순서 변경)
- ✅ 탭 컨텍스트 메뉴
- ✅ 탭 닫기/고정
- ✅ Sticky 탭
- ✅ 경로 축약 (shorten)
- ✅ 보조 창 분리 (새 창으로 열기)
- ✅ 키보드 단축키
- ✅ **탭 래핑 (여러 줄)** - P2 완료
- ✅ **탭 스크롤 버튼** - P2 완료
- ✅ **브레드크럼 네비게이션** - P2 완료

## 완전 매핑 (✅)

| VS Code | 현재 구현 |
|---------|----------|
| EditorTabsControl.create() | EditorTabsControl.tsx (useEffect) |
| EditorTabsControl.tabHeight | EDITOR_TAB_HEIGHT 상수 |
| EditorTabsControl.getContextMenuActions() | contextMenuService.ts |
| MultiEditorTabsControl.handleDrop() | dnd.ts + 드래그 핸들러 |
| MultiEditorTabsControl.computeTabLabels() | getUniqueTabLabels() |
| AuxiliaryWindowService.open() | useAuxiliaryWindowReceiver.ts |
| ContextKeyService | useContextKey.tsx |
| labels.shorten() | shorten() 유틸리티 |

## 부분 매핑 (⚠️)

| VS Code | 현재 구현 | 갭 |
|---------|----------|-----|
| tabActionsBar | EditorActionsToolbar | 일부 액션만 구현 |

## 미구현 (❌)

| 기능 | VS Code 소스 | 우선순위 |
|------|-------------|----------|
| 싱글 탭 모드 | singleEditorTabsControl.ts | P3 |
| 탭 없음 모드 | noEditorTabsControl.ts | P3 |

*모든 P2 기능 구현 완료 (Phase 5)*

## 매핑 상세

### editorTabsControl.ts → EditorTabsControl.tsx

```
✅ constructor → React props + useTabsStore()
✅ create() → useEffect 초기화
✅ tabHeight → EDITOR_TAB_HEIGHT 상수
✅ onTabClick() → handleTabClick()
✅ onTabClose() → handleTabClose()
✅ onContextMenu() → handleContextMenu()
✅ handleDrop() → onDrop 이벤트 + moveEditor()
✅ layout() → 반응형 CSS (Tailwind)
✅ tabWrapping → data-wrap 속성 + flex-wrap: wrap (P2)
✅ scrollButtons → tabs-scroll-button 컴포넌트 (P2)
✅ wheelScroll → handleWheel + deltaY → scrollLeft (P2)
```

### multiEditorTabsControl.ts → tabsStore.ts + 컴포넌트

```
✅ computeTabLabels() → getUniqueTabLabels()
✅ handleDrop() → moveEditor() + reorderEditors()
✅ stickyCount → tabsStore.stickyCount
✅ pinEditor/unpinEditor → pinEditor()
✅ scrollbar → tabs-scroll-wrapper (P2)
✅ showTabsScrollbar → canScrollLeft/canScrollRight 상태 (P2)
✅ checkOverflow() → 오버플로우 감지 (P2)
```

### breadcrumbs.ts → EditorBreadcrumbs.tsx (P2)

```
✅ BreadcrumbSegment → BreadcrumbSegment 인터페이스
✅ parsePath() → useMemo 내 경로 파싱
✅ onSegmentClick() → handleClick 콜백
✅ getFileIcon() → 확장자별 아이콘 매핑
```

### auxiliaryWindowService.ts → useAuxiliaryWindowReceiver.ts

```
✅ open() → window.electronAPI.editor.openAuxiliaryWindow()
✅ onDispose → useEffect cleanup
✅ windowReady → IPC 통신
✅ handleMessage → onAuxiliaryWindowMessage 이벤트
```

## 상세 문서 참조

- [소스-코드-매핑-완전판.md](소스-코드-매핑-완전판.md) - 38개 메서드 1:1 매핑
- [CSS-매핑.md](CSS-매핑.md) - CSS 스타일 매핑
