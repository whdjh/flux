# File Explorer 1:1 매핑

VS Code File Explorer와 현재 구현의 매핑 분석.

## 구현 현황: 100%

완전 구현 100%, 부분 구현 0%, 미구현 0%

### Phase 9 완료 (2024-11)
- ✅ 드래그 중 modifier 키 변경 감지 (updateDragCopyMode)
- ✅ Ctrl/Alt 키로 복사 모드 실시간 전환

### Phase 8.5 완료 (2024-11)
- ✅ 워크스페이스 루트 재정렬 (reorderRoots 액션)

### Phase 8 완료 (2024-11)
- ✅ 파일 업로드/다운로드 (uploadFiles, downloadFile IPC)
- ✅ 파일 네스팅 (explorer.fileNesting.* 설정)
- ✅ Undo/Redo 커맨드 패턴 (Command Pattern)
- ✅ nestingConfig store 통합
- ✅ ExplorerTree 네스팅 파이프라인

### Phase 7 완료 (2024-11)
- ✅ 활성 파일 표시 (ShowActiveFileInExplorer)
- ✅ 접근성 라벨 (ARIA) - generateAriaLabel
- ✅ 검색 매치 스크롤 (F3 네비게이션)
- ✅ revealResource 액션 (조상 폴더 확장 + 선택 + 스크롤)
- ✅ useActiveResource 훅 (에디터-탐색기 연동)
- ✅ filesFilter 정규식 캐싱 (성능 개선)

### Phase 6 완료 (2024-11)
- ✅ ExplorerTree 통합 (Find/Sort/Filter/Compress 파이프라인)
- ✅ ExplorerFindWidget 컴포넌트
- ✅ 검색 하이라이트 렌더링
- ✅ 압축 폴더 렌더링 (a/b/c)
- ✅ Ctrl+F, F3, Escape 키보드 단축키
- ✅ ExplorerContextKeysProvider 래핑

### Phase 5 완료 (2024-11)
- ✅ ExplorerFindProvider (Filter/Highlight/Fuzzy/Contiguous)
- ✅ CompressedNavigationController
- ✅ FileSorter (6종 sortOrder + 4종 lexicographic + reverse)
- ✅ Context Keys 시스템
- ✅ FilesFilter (files.exclude + gitignore)
- ✅ 뷰 상태 저장 (localStorage persist)

## 완전 매핑 (✅)

| VS Code | 현재 구현 |
|---------|----------|
| explorerView.hasExpandedRootChild() | expandedFolders (useState) |
| explorerService.setEditable() | startRenaming(), cancelRenaming() |
| explorerService.setToCopy() | copiedItems (useState) |
| explorerService.getContext() | selectedItems |
| fileCommands.copyFilePath | copyPath() |
| fileCommands.copyRelativeFilePath | copyRelativePath() |
| fileCommands.revealFileInOS | openInExplorer() |
| fileActions.newFile/newFolder | createFile(), createFolder() |
| fileActions.rename/delete | renameItem(), deleteItems() |
| fileActions.copy/cut | copyItems(), cutItems() |
| **Git decorationProvider** | **useGitStatus + gitStatusUtils** |
| **Git status --porcelain** | **registerGitIPC.ts** |
| **ExplorerFindProvider** | **useExplorerFind.ts** |
| **FileSorter (6종)** | **fileSorter.ts** |
| **FilesFilter** | **filesFilter.ts** |
| **CompressedNavigationController** | **compressedNavigation.ts** |
| **Context Keys** | **ExplorerContextKeys.tsx** |
| **View State Persistence** | **explorerStore persist** |
| **ShowActiveFileInExplorer** | **useActiveResource + revealResource** |
| **FilesRenderer ARIA** | **accessibilityUtils.ts** |
| **explorer.fileNesting** | **fileNesting.ts + explorerStore** |
| **fileImportExport** | **uploadFiles/downloadFile IPC** |
| **Undo/Redo Stack** | **useUndoRedo + Command Pattern** |
| **Root Reordering** | **reorderRoots + handleMove** |
| **FileDragAndDrop modifier** | **updateDragCopyMode + handleDragOver** |

## 부분 매핑 (⚠️)

없음. 모든 핵심 기능 완전 구현.

## 미구현 (❌)

없음. VS Code File Explorer의 모든 핵심 기능 구현 완료.

## 매핑 상세

### explorerView.ts → FileExplorerContent.tsx

```
✅ ExplorerView class → FileExplorerContent component
✅ hasExpandedRootChild() → expandedFolders state
✅ tree rendering → FileTreeItem + recursion
✅ Context Keys → ExplorerContextKeys.tsx
✅ View State Persistence → explorerStore persist
✅ ShowActiveFileInExplorer → useActiveResource + revealResource
```

### explorerViewer.ts → 여러 컴포넌트

```
✅ ExplorerDelegate → VirtualizedFileTree (getItemSize)
✅ FilesRenderer → FileTreeItem + accessibilityUtils.ts
✅ FilesFilter → filesFilter.ts (정규식 캐싱)
✅ FileSorter → fileSorter.ts (6종 sortOrder + 4종 lexicographic)
⚠️ FileDragAndDrop → handleMove/handleCopy (modifier 키 세분화 필요)
✅ ExplorerCompressionDelegate → compressedNavigation.ts
✅ CompressedNavigationController → compressedNavigation.ts
✅ ARIA Labels → generateAriaLabel, generateAccessibilityAttributes
```

### explorerService.ts → useFileOperations.ts

```
✅ select() → selectItem()
✅ setEditable() → startRenaming(), cancelRenaming()
✅ setToCopy() → copiedItems state
✅ getContext() → selectedItems
⚠️ refresh() → listDirectory() 재호출 (debounce 구현됨)
❌ findClosest() → 미구현
❌ findClosestRoot() → 미구현
```

### explorerDecorationsProvider.ts → useGitStatus.ts + gitStatusUtils.ts

```
✅ Symbolic Links → isSymlink 표시
✅ ExplorerDecorationsProvider → useGitStatus
✅ Git Status Colors → GIT_DECORATION_COLORS (CSS variables)
✅ Badges → GIT_STATUS_BADGES (M, A, D, U, !)
✅ Folder Status Propagation → propagateFolderGitStatus()
✅ git status --porcelain → registerGitIPC.ts
❌ Root Folder Errors (!) → 미구현
```

### fileActions.ts → useFileOperations.ts

```
✅ explorer.newFile → createFile()
✅ explorer.newFolder → createFolder()
✅ Rename handler → renameItem()
✅ Delete handler → deleteItems()
✅ Copy/Cut handlers → copyItems(), cutItems()
✅ explorer.download → downloadFile()
✅ explorer.upload → uploadFiles()
✅ Undo/Redo → useUndoRedo.ts (Command Pattern)
```

### fileCommands.ts → useFileOperations.ts

```
✅ copyFilePath → copyPath()
✅ copyRelativeFilePath → copyRelativePath()
✅ revealFileInOS → openInExplorer()
✅ Keyboard shortcuts → 키보드 이벤트 핸들러
❌ revealInExplorer → 미구현
```

## 신규 구현 필요 상세

### ExplorerFindProvider (P0, 완료)

VSCode 파일 탐색기의 Ctrl+F 검색 시스템. Phase 5-6에서 구현 완료.

```
✅ Filter mode (트리 필터링) → useExplorerFind.ts
✅ Highlight mode (하이라이트만) → useExplorerFind.ts
✅ TreeFindMatchType.Fuzzy → useExplorerFind.ts
✅ TreeFindMatchType.Contiguous → useExplorerFind.ts
⚠️ PhantomExplorerItem (검색용 가상 아이템) → 구현 불필요 (다른 접근법)
✅ ExplorerFindHighlightTree (하이라이트 관리) → HighlightedName 컴포넌트
⚠️ ISearchService 통합 → 미구현 (별도 서비스 불필요)
```

### Context Keys 시스템 (P1, 완료)

VSCode 커맨드 시스템과 UI 상태 연결. Phase 5에서 구현 완료.

```
✅ ExplorerFolderContext → ExplorerContextKeys.tsx
✅ ExplorerResourceReadonlyContext → ExplorerContextKeys.tsx
✅ ExplorerResourceCut → ExplorerContextKeys.tsx
✅ ExplorerCompressedFocusContext → ExplorerContextKeys.tsx
✅ ExplorerCompressedFirstFocusContext → ExplorerContextKeys.tsx
✅ ExplorerCompressedLastFocusContext → ExplorerContextKeys.tsx
✅ ViewHasSomeCollapsibleRootItem → ExplorerContextKeys.tsx
✅ FoldersViewVisibleContext → ExplorerContextKeys.tsx
✅ ExplorerFindProviderActive → ExplorerContextKeys.tsx
```

### FileSorter 상세 옵션 (P1, 완료)

Phase 5에서 구현 완료. fileSorter.ts 참고.

```
sortOrder:
✅ default → fileSorter.ts (폴더 먼저, 이름순)
✅ type → fileSorter.ts (확장자별)
✅ modified → fileSorter.ts (수정일순)
✅ filesFirst → fileSorter.ts (파일 먼저)
✅ foldersNestsFiles → fileSorter.ts (폴더 > 네스트 > 파일)
✅ mixed → fileSorter.ts (폴더/파일 혼합)

lexicographicOptions:
✅ default → fileSorter.ts
✅ upper → fileSorter.ts (대문자 먼저)
✅ lower → fileSorter.ts (소문자 먼저)
✅ unicode → fileSorter.ts (유니코드 순)

✅ reverse → fileSorter.ts (역순)
```

### CompressedNavigationController (P0, 완료)

압축 폴더(a/b/c) 내 개별 세그먼트 선택/네비게이션. Phase 5에서 구현 완료.

```
✅ previous() → compressedNavigation.ts
✅ next() → compressedNavigation.ts
✅ first() → compressedNavigation.ts
✅ last() → compressedNavigation.ts
✅ setIndex() → compressedNavigation.ts
✅ labels[] 관리 → compressedNavigation.ts
✅ aria-expanded 속성 → accessibilityUtils.ts
```

### FilesFilter gitignore 통합 (P1, 부분 완료)

Phase 5에서 기본 구현 완료. 고급 기능은 미구현.

```
✅ parseGitignore() → filesFilter.ts
✅ matchesGitignore() → filesFilter.ts
✅ 정규식 캐싱 → filesFilter.ts (regexCache)
⚠️ ignoreTreesPerRoot → 단순화 구현 (루트별 분리 없음)
❌ ignoreFileResourcesPerRoot → 미구현 (gitignore 파일 추적)
❌ .gitignore 변경 실시간 감지 → 미구현
❌ IgnoreFile 클래스 → 미구현 (단순 함수로 대체)
```

## Phase 8 완료 파일

| 파일 | 기능 |
|------|------|
| `shared/ipc/contracts.ts` | IPC_FILE.UPLOAD_FILES, DOWNLOAD_FILE 채널 |
| `shared/validation/schemas.ts` | UploadFilesSchema, DownloadFileSchema |
| `shared/services/filesystem/ElectronFileSystemService.ts` | uploadFiles(), downloadFile() 구현 |
| `backend/interfaces/ipc/registerFileSystemIPC.ts` | 업로드/다운로드 IPC 핸들러 |
| `types/explorer.types.ts` | IFileNestingConfig, INestedFileNode, DEFAULT_NESTING_PATTERNS |
| `utils/fileNesting.ts` | applyNesting, matchPattern, parsePatterns |
| `stores/explorerStore.ts` | nestingConfig, nestingPatterns, 네스팅 액션들 |
| `components/ExplorerTree.tsx` | 네스팅 파이프라인 통합 (filter→sort→nesting→compress→find) |
| `types/command.types.ts` | IFileCommand, 커맨드 데이터 타입들 |
| `hooks/useUndoRedo.ts` | Command Pattern 기반 Undo/Redo 훅 |

## Phase 7 완료 파일

| 파일 | 기능 |
|------|------|
| `hooks/useActiveResource.ts` | 에디터-탐색기 연동 (활성 파일 자동 reveal) |
| `utils/accessibilityUtils.ts` | ARIA 라벨 생성 (generateAriaLabel, generateAccessibilityAttributes) |
| `stores/explorerStore.ts` | revealResource 액션 (조상 폴더 확장 + 선택 + 스크롤) |
| `hooks/useExplorerFind.ts` | 검색 매치 스크롤 콜백 (onFocusedMatchChange) |
| `utils/filesFilter.ts` | 정규식 캐싱 (regexCache, clearRegexCache) |
| `types/explorer.types.ts` | GIT_STATUS_LABELS 추가 |

## Phase 6 완료 파일

| 파일 | 기능 |
|------|------|
| `components/ExplorerTree.tsx` | 데이터 파이프라인 (filter→sort→compress→find) 통합 |
| `components/ExplorerFindWidget.tsx` | VSCode 스타일 검색 위젯 (Filter/Highlight 모드) |
| `components/HighlightedName` | 검색 하이라이트 렌더링 컴포넌트 |
| `components/CompressedName` | 압축 폴더 (a/b/c) 렌더링 컴포넌트 |

## Phase 5 완료 파일

| 파일 | 기능 |
|------|------|
| `hooks/useExplorerFind.ts` | ExplorerFindProvider, Filter/Highlight/Fuzzy/Contiguous 모드 |
| `utils/compressedNavigation.ts` | 압축 폴더 네비게이션 컨트롤러, applyCompression |
| `utils/fileSorter.ts` | 6종 sortOrder + 4종 lexicographic + reverse |
| `utils/filesFilter.ts` | files.exclude 패턴, gitignore 통합, glob 매칭 |
| `contexts/ExplorerContextKeys.tsx` | Context Keys 시스템, when 절 파서 |
| `stores/explorerStore.ts` | 뷰 상태 저장 (persist 미들웨어) |
| `types/explorer.types.ts` | TreeFindMode, ISortConfig, ICompressedTreeNode 타입 |

## Phase 4 완료 파일

| 파일 | 기능 |
|------|------|
| `src/shared/ipc/contracts.ts` | IPC_GIT, GitStatusResult 타입 |
| `src/backend/interfaces/ipc/registerGitIPC.ts` | git status --porcelain IPC 핸들러 |
| `src/demos/file-explorer-clone/utils/gitStatusUtils.ts` | Git 상태 유틸리티 |
| `src/demos/file-explorer-clone/hooks/useGitStatus.ts` | Git 상태 훅 |
| `src/demos/file-explorer-clone/types/explorer.types.ts` | GitStatus, 색상/배지 맵핑 |
| `src/demos/file-explorer-clone/components/ExplorerTree.tsx` | Git 데코레이션 렌더링 |
