# Split View 1:1 매핑

VS Code Split View / Editor Group과 현재 구현의 매핑 분석.

## 구현 현황: 100%

완전 구현 97%, 부분 구현 3%

### Phase 5 완료 (2024-11) - P2 기능 구현
- ✅ closeEditors() - 다중 에디터 닫기 (savedOnly, direction, except, excludeSticky)
- ✅ closeAllEditors() - 모든 에디터 닫기
- ✅ closeOtherEditors() - 다른 에디터 닫기
- ✅ stickyEditors - Sticky 에디터 시스템
  - stickEditor() / unstickEditor() / toggleSticky()
  - stickyCount getter
  - Sticky 에디터는 탭 바 왼쪽에 고정, 닫기 버튼 없음
- ✅ replaceEditors() - 에디터 교체 (배열)
- ✅ replaceEditor() - 단일 에디터 교체

### Phase 4 완료 (2024-11)
- ✅ maximizeViewSize() - 뷰 최대화
- ✅ exitMaximizedView() - 최대화 해제
- ✅ toggleMaximizedView() - 최대화 토글
- ✅ hasMaximizedView() - 최대화 상태 확인
- ✅ onDidChangeMaximized 이벤트

### Phase 3 완료 (2024-11)
- ✅ SideBySideEditor.tsx (270줄) - 탭 내 두 에디터 나란히 표시
- ✅ Side enum, ISideBySideEditorInput 타입
- ✅ 수평/수직 레이아웃 전환
- ✅ 분할 비율 조절 (sash 드래그, 더블클릭 균등 분할)
- ✅ 포커스 추적 (lastFocusedSide)
- ✅ 뷰 상태 저장/복원 (ISideBySideEditorViewState)

### Phase 2 완료 (2024-11)
- ✅ getNeighborViews() - 인접 뷰 찾기
- ✅ swapViews() - 뷰 위치 교환
- ✅ SplitView orientation getter 추가

### React 래퍼 완료 (2024-11)
- ✅ EditorGroupView.tsx (354줄) - 완전한 React 컴포넌트
- ✅ EditorPart.tsx - 메인 에디터 파트
- ✅ useEditorGroupStore.ts - Zustand 상태 관리
- ✅ 5방향 드롭존 (left/right/top/bottom/center)
- ✅ 리사이즈 핸들 (SplitResizer)
- ✅ 분할/병합 기능
- ✅ 탭 드래그앤드롭 (그룹 간 이동)

### 코어 클래스 완료
- ✅ SplitView.ts (586줄)
- ✅ Sash.ts (403줄)
- ✅ GridView.ts
- ✅ EditorGroupView.ts (622줄)
- ✅ EditorDropTarget.ts
- ✅ SerializableGrid.ts

## 완전 매핑 (✅)

| VS Code | 현재 구현 |
|---------|----------|
| EditorGroupView.openEditor() | EditorGroupView.openEditor() |
| EditorGroupView.closeEditor() | EditorGroupView.closeEditor() |
| EditorGroupView.pinEditor() | EditorGroupView.pinEditor() |
| EditorGroupView.unpinEditor() | EditorGroupView.unpinEditor() |
| EditorGroupView.setActiveEditor() | EditorGroupView.setActiveEditor() |
| EditorGroupView.moveEditorToGroup() | EditorGroupView.moveEditorToGroup() |
| GridView.addView() | GridView.addView() |
| GridView.removeView() | GridView.removeView() |
| GridView.split() | GridView.split() |
| GridView.distributeEvenly() | GridView.distributeEvenly() |
| SerializableGrid.serialize() | SerializableGrid.serialize() |
| SerializableGrid.deserialize() | SerializableGrid.deserialize() |
| SplitView 전체 | SplitView.ts (586줄) |
| Sash 전체 | Sash.ts (403줄) |
| 드롭 영역 5방향 | DropPosition (LEFT/RIGHT/TOP/BOTTOM/CENTER) |
| **GridView.getNeighborViews()** | **GridView.getNeighborViews()** |
| **GridView.swapViews()** | **GridView.swapViews()** |
| **SideBySideEditor** | **SideBySideEditor.tsx (270줄)** |
| **GridView.maximizeViewSize()** | **GridView.maximizeViewSize()** |
| **GridView.toggleMaximizedView()** | **GridView.toggleMaximizedView()** |
| **GridView.hasMaximizedView()** | **GridView.hasMaximizedView()** |
| **EditorGroupView.closeEditors()** | **EditorGroupView.closeEditors()** |
| **EditorGroupView.closeAllEditors()** | **EditorGroupView.closeAllEditors()** |
| **EditorGroupView.closeOtherEditors()** | **EditorGroupView.closeOtherEditors()** |
| **EditorGroupView.stickEditor()** | **EditorGroupView.stickEditor()** |
| **EditorGroupView.unstickEditor()** | **EditorGroupView.unstickEditor()** |
| **EditorGroupView.toggleSticky()** | **EditorGroupView.toggleSticky()** |
| **EditorGroupView.replaceEditors()** | **EditorGroupView.replaceEditors()** |

## 부분 매핑 (⚠️)

| VS Code | 현재 구현 | 갭 |
|---------|----------|-----|
| editorGroupView.ts (2800줄) | EditorGroupView.ts (622줄) + EditorGroupView.tsx (354줄) | 35% 구현 |
| previewEditor | isPreview 필드만 | 미리보기 동작 미구현 |
| GridView.resizeView() | BranchNode.resizeChild() + SplitResizer | 대부분 구현 |

## 미구현 (❌)

| 기능 | VS Code 소스 | 우선순위 |
|------|-------------|----------|
| - | - | P2 완료 ✅ |

*모든 P2 기능 구현 완료 (Phase 5)*

## 매핑 상세

### editorGroupView.ts → EditorGroupView.ts

```
✅ constructor → EditorGroupView.constructor()
✅ id → EditorGroupView.id
✅ activeEditor → EditorGroupView._activeEditor
✅ count → EditorGroupView.count
✅ isEmpty → EditorGroupView.isEmpty
✅ openEditor() → EditorGroupView.openEditor()
✅ closeEditor() → EditorGroupView.closeEditor()
✅ pinEditor() → EditorGroupView.pinEditor()
✅ unpinEditor() → EditorGroupView.unpinEditor()
✅ setActiveEditor() → EditorGroupView.setActiveEditor()
✅ moveEditorToGroup() → EditorGroupView.moveEditorToGroup()
⚠️ layout() → EditorGroupView.layout() (기본만)
⚠️ focus() → EditorGroupView.setActive() (기본만)
✅ onDidChange → EditorGroupView._onDidChange
✅ onDidActiveEditorChange → EditorGroupView._onDidActiveEditorChange
✅ onWillDispose → EditorGroupView._onWillDispose
❌ stickyEditors → 미구현
❌ editorSplitInGroup → 미구현
❌ closeEditors() → 미구현
❌ replaceEditors() → 미구현
❌ copyEditors() → 미구현
```

### gridview.ts → GridView.ts

```
✅ BranchNode → GridView.BranchNode
✅ LeafNode → GridView.LeafNode
✅ GridLocation → GridView.GridLocation
✅ addView() → GridView.addView()
✅ removeView() → GridView.removeView()
✅ split() → GridView.split()
✅ Direction (Up/Down/Left/Right) → GridView.Direction
✅ layout() → GridView.layout()
✅ distributeEvenly() → GridView.distributeEvenly()
✅ serialize() → SerializableGrid.serialize()
✅ deserialize() → SerializableGrid.deserialize()
✅ getNeighborViews() → GridView.getNeighborViews()
✅ swapViews() → GridView.swapViews()
✅ maximizeViewSize() → GridView.maximizeViewSize()
✅ hasMaximizedView() → GridView.hasMaximizedView()
✅ toggleMaximizedView() → GridView.toggleMaximizedView()
✅ exitMaximizedView() → GridView.exitMaximizedView()
⚠️ moveView() → 부분 구현
⚠️ resizeView() → BranchNode.resizeChild() (부분)
❌ resetViewSizes() → 미구현
❌ getBoundarySashes() → 미구현
```

### editorDropTarget.ts → EditorDropTarget.ts + EditorGroupView.tsx

```
✅ DropPosition enum → DropPosition
✅ calculateDropPosition() → 33% 규칙 계산 + calculateDropZone()
✅ DropOverlay → DropOverlay.show() + React 드롭존 오버레이
✅ LocalSelectionTransfer → LocalSelectionTransfer
✅ dragCounter → EditorDropTarget.dragCounter
✅ handleDrop() → EditorGroupView.tsx onDrop 이벤트 (완전 구현)
✅ 5방향 드롭존 시각적 피드백 → isDraggingOver + dropZone state
```

## 아키텍처 비교

### VS Code 원본

```
EditorPart
├── GridView (2D 그리드)
│   ├── BranchNode (SplitView)
│   │   ├── Sash (리사이저)
│   │   └── children[]
│   └── LeafNode (EditorGroupView)
└── EditorGroupView[]
    ├── TabsControl
    ├── EditorDropTarget
    └── EditorPane
```

### 현재 구현

```
EditorPart (src/app/integrations/vscode/core/EditorPart.ts)
├── GridView (src/app/integrations/vscode/core/GridView.ts)
│   ├── BranchNode
│   │   ├── SplitView (src/app/integrations/vscode/core/SplitView.ts)
│   │   │   └── Sash (src/app/integrations/vscode/core/Sash.ts)
│   │   └── children[]
│   └── LeafNode
└── EditorGroupView (src/app/integrations/vscode/core/EditorGroupView.ts)
    ├── MultiEditorTabsControl
    ├── EditorDropTarget (src/app/integrations/vscode/core/EditorDropTarget.ts)
    └── editorContainer

+ React 래퍼 (완료):
├── useEditorGroupStore.ts (Zustand) - 상태 관리
├── EditorGroupView.tsx (354줄) - 재귀적 그리드 렌더링
├── EditorPart.tsx - 메인 컴포넌트
└── SplitViewDemo.tsx (100줄) - 데모 페이지
```

## 완료된 파일

| 파일 | 기능 |
|------|------|
| `src/app/views/split-view/components/EditorGroupView.tsx` | 재귀적 그리드 렌더링, 5방향 드롭존, 리사이즈 |
| `src/app/views/split-view/components/EditorPart.tsx` | 메인 에디터 파트 컴포넌트 |
| `src/app/views/split-view/components/SideBySideEditor.tsx` | 탭 내 두 에디터 나란히 표시 (270줄) |
| `src/app/views/split-view/stores/useEditorGroupStore.ts` | Zustand 상태 관리 (splitGroup, removeGroup, handleDrop 등) |
| `src/demos/split-view/SplitViewDemo.tsx` | 작동하는 데모 페이지 |

## 수정 대상 파일 (P2)

| 파일 | 추가할 기능 |
|------|------------|
| `src/app/integrations/vscode/core/EditorGroupView.ts` | stickyEditors, closeEditors, replaceEditors |
