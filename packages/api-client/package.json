{
  "name": "@flux/api-client",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'TODO: setup build'",
    "lint": "echo 'TODO: setup linter'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.4",
    "msw": "^2.6.8"
  },
  "dependencies": {
    "@flux/shared": "workspace:*",
    "zod": "^4.3.6"
  }
}
